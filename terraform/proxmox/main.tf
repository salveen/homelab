# Intentionally empty. Resources live in vms.tf, providers in providers.tf,
# variables in variables.tf, outputs in outputs.tf. This file exists so
# `terraform init` finds *something* even before vms.tf is filled in, and as a
# conventional landing spot for future top-level data sources.
